{
  "name": "financial-plan",
  "version": "0.0.0",
  "private": true,
  "description": "Not a package and not a build step — this file exists so Dependabot has a manifest to read. Its entries are dependencies this app loads at RUNTIME but never installs: the vendored chart.min.js beside the app, and the Firebase SDK the page loads from www.gstatic.com. Nothing is ever installed from it (CI passes --omit=dev). See README.md.",
  "license": "MIT",
  "devDependencies": {
    "chart.js": "4.4.1",
    "firebase": "12.18.0"
  }
}
