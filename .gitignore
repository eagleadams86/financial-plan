# Real financial data must NEVER be committed — this repo is public.
# The import script and tests read these locally; they stay on this Mac.
financial-plan-data.json
expected-2026.json
# whatever migrate_local_data.py / merge_debit_card.py write beside them
*-migrated.json
*-debit-card.json
# Anything personal that belongs beside the code but never in it — API keys,
# scratch notes. Ignoring the whole directory means a file added here later is
# covered without anyone having to remember to ignore it.
.local/
*-report*.txt
.DS_Store
node_modules/
package-lock.json
